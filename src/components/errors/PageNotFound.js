import React from 'react';
import Typography from '@material-ui/core/Typography';

function PageNotFound(props) {
  return <Typography variant="h2" className="title">{props.title}</Typography>
}

export default PageNotFound;